package.dir = package

lib.dir = lib
build.dir = ${lib.dir}/build
mandatory.dir = ${lib.dir}/mandatory
src.dir = src/main/java
src.tests.unit.dir = src/test/java

temp.dir = temp
temp.dir.classes = ${temp.dir}/classes

package.name = br.com.maplink.webservices.places.client
package.file = ${package.dir}/${package.name}

temp.dir.tests.unit = ${temp.dir}/test/unit
temp.dir.tests.unit.classes = ${temp.dir.tests.unit}/classes