package.dir = package

src.dir = src/main/java
src.tests.unit.dir = src/test/java
lib.src.dir = src/main/lib
lib.test.dir = src/test/lib

version = version

temp.dir = temp
temp.dir.classes = ${temp.dir}/src/classes

tests.report = ${temp.dir}/test-results

package.name = br.com.maplink.webservices.places.client
package.file = ${package.dir}/${package.name}

temp.dir.tests.unit = ${temp.dir}/test
temp.dir.tests.unit.classes = ${temp.dir.tests.unit}/classes